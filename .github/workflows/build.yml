name: Build

on:
  push:
    branches:
      - main
      - master

jobs:
  run_infra:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: setup
        run: pwd && ls -lR
      - name: Do it
        uses: magnayn-mns/infradsl@master
        with:
          file: /github/home/script.infra.kts
