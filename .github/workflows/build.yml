name: Build

on:
  push:
    branches:
      - main
      - master

jobs:
  run_infra:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Do it
        uses: magnayn-mns/infradsl@master
        with:
          file: /github/workflow/script.infra.kts
